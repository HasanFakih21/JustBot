[package]
name = "justbot"
version = "0.4.0"
edition = "2024"
build = "build.rs"

[profile.release]
lto = "fat"
panic = "abort"
codegen-units = 1

[dependencies]
rand = { version = "0.10.2", optional = true }

[features]
default = []
tuning = []
datagen = ["dep:rand"]
